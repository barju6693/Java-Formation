package com.ejemplos.model;

public class ExemplePolimorfisme {

	/*public void polimorf(){
		Treballador treb = new Treballador();
		DaoCRUD dao = new DaoCRUD();
		
		dao.create(treb);
		dao.delete(treb);
		dao.read(treb);
		dao.search(treb);
		dao.update(treb);
		
		IReadDao<Treballador> read = new DaoCRUD(); //only contains read
		read.read(treb);
		read.search(treb);
		((DaoCRUD)read).update(treb);
	}*/
	
}
